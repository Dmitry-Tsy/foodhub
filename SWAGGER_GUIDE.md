# 📖 Swagger UI - Документация и тестирование API

## 🚀 Быстрый доступ

### **Localhost (для разработки):**
```
http://localhost:3000/api-docs
```

### **Реальное устройство:**
```
http://192.168.31.212:3000/api-docs
```

---

## 🎯 Что такое Swagger?

**Swagger UI** - это интерактивная веб-документация для вашего API, которая позволяет:

✅ **Просматривать** все доступные эндпоинты  
✅ **Тестировать** API прямо в браузере  
✅ **Видеть** примеры запросов и ответов  
✅ **Авторизоваться** и делать защищенные запросы  
✅ **Экспортировать** документацию в OpenAPI формат

---

## 📱 Как использовать Swagger UI

### Шаг 1: Откройте Swagger UI

1. Запустите бэкенд:
   ```bash
   cd backend
   npm run dev
   ```

2. Откройте в браузере:
   ```
   http://localhost:3000/api-docs
   ```

### Шаг 2: Зарегистрируйтесь

1. Найдите раздел **Auth**
2. Раскройте `POST /api/auth/register`
3. Нажмите **"Try it out"**
4. Заполните поля:
   ```json
   {
     "username": "testuser",
     "email": "test@example.com",
     "password": "password123"
   }
   ```
5. Нажмите **"Execute"**
6. **Скопируйте токен** из ответа (в поле `token`)

### Шаг 3: Авторизуйтесь

1. Нажмите кнопку **"Authorize"** (замочек) в верхней части страницы
2. В поле `Value` вставьте: `Bearer ВАШ_ТОКЕН`
   ```
   Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```
3. Нажмите **"Authorize"**
4. Закройте окно

### Шаг 4: Тестируйте защищенные эндпоинты

Теперь вы можете тестировать любые эндпоинты, требующие авторизацию:

- ✅ GET `/api/auth/me` - Получить свой профиль
- ✅ POST `/api/dishes` - Добавить блюдо
- ✅ POST `/api/taste-profile` - Создать вкусовой профиль
- ✅ И многие другие...

---

## 🔍 Основные разделы API

### 🔐 **Auth** - Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получить профиль (требует токен)
- `PUT /api/auth/profile` - Обновить профиль (требует токен)

### 🍽️ **Dishes** - Блюда
- `GET /api/dishes/restaurant/{restaurantId}` - Меню ресторана
- `GET /api/dishes/{dishId}` - Получить блюдо
- `POST /api/dishes` - Добавить блюдо (требует токен)
- `PUT /api/dishes/{dishId}` - Обновить блюдо (требует токен)
- `DELETE /api/dishes/{dishId}` - Удалить блюдо (требует токен)

### 📝 **Reviews** - Отзывы
- `GET /api/dishes/{dishId}/reviews` - Отзывы на блюдо
- `POST /api/dishes/{dishId}/reviews` - Добавить отзыв (требует токен)

### 🏪 **Restaurants** - Рестораны
- `GET /api/restaurants/{restaurantId}` - Получить ресторан
- `GET /api/restaurants/search` - Поиск ресторанов
- `POST /api/restaurants` - Создать ресторан (требует токен)

### 👅 **Taste Profile** - Вкусовые профили
- `GET /api/taste-profile` - Получить профиль (требует токен)
- `POST /api/taste-profile` - Создать/обновить (требует токен)
- `DELETE /api/taste-profile` - Удалить профиль (требует токен)

---

## 💡 Полезные функции Swagger UI

### 1. **Try it out** - Попробовать
Кнопка позволяет редактировать параметры и тело запроса

### 2. **Execute** - Выполнить
Отправляет реальный запрос к API

### 3. **Responses** - Ответы
Показывает примеры ответов для каждого статус-кода

### 4. **Models** - Модели
Показывает структуру данных (User, Dish, Restaurant и т.д.)

### 5. **Curl** - Копировать как curl
После выполнения запроса можно скопировать его в формате curl

---

## 🎨 Примеры использования

### Пример 1: Полный цикл добавления блюда

**1. Регистрация:**
```
POST /api/auth/register
{
  "username": "chef",
  "email": "chef@restaurant.com",
  "password": "chef2025"
}
```

**2. Авторизация (скопировать токен)**

**3. Создать ресторан:**
```
POST /api/restaurants
{
  "name": "Ресторан \"У шефа\"",
  "address": "Москва, ул. Примерная 1",
  "latitude": 55.7558,
  "longitude": 37.6173,
  "cuisineType": "Авторская"
}
```

**4. Добавить блюдо (используя restaurant ID из ответа):**
```
POST /api/dishes
{
  "name": "Стейк рибай",
  "description": "Сочный стейк из мраморной говядины",
  "restaurantId": "uuid-из-предыдущего-ответа",
  "price": 2500,
  "category": "Основные блюда"
}
```

**5. Добавить отзыв:**
```
POST /api/dishes/{dishId}/reviews
{
  "rating": 9.5,
  "comment": "Лучший стейк в городе!",
  "foodPairing": "Рекомендую с красным вином"
}
```

### Пример 2: Настройка вкусового профиля

```
POST /api/taste-profile
{
  "favoriteCuisines": ["Итальянская", "Японская", "Грузинская"],
  "favoriteIngredients": ["Морепродукты", "Паста", "Сыр"],
  "excludedIngredients": ["Орехи", "Кинза"],
  "spicyLevel": "medium",
  "dietaryRestrictions": ["Без глютена"],
  "preferredPriceRangeMin": 500,
  "preferredPriceRangeMax": 3000,
  "tastePreferences": {
    "sweet": 6,
    "salty": 7,
    "sour": 5,
    "bitter": 4,
    "umami": 9
  }
}
```

---

## 🔧 Troubleshooting

### ❌ "Failed to fetch"
**Причина:** Сервер не запущен  
**Решение:**
```bash
cd backend
npm run dev
```

### ❌ "401 Unauthorized"
**Причина:** Нужна авторизация  
**Решение:**
1. Зарегистрируйтесь или войдите
2. Скопируйте токен
3. Нажмите "Authorize" и вставьте `Bearer ВАШ_ТОКЕН`

### ❌ "404 Not Found"
**Причина:** Неправильный ID ресторана/блюда  
**Решение:** Проверьте что объект существует в базе

### ❌ Swagger не открывается
**Причина:** Неправильный URL  
**Решение:** Убедитесь что используете `/api-docs` а не `/api`

---

## 🌐 Доступ с реального устройства

1. Убедитесь что устройство и компьютер в одной сети Wi-Fi
2. Откройте браузер на телефоне
3. Перейдите на: `http://192.168.31.212:3000/api-docs`
4. Тестируйте API прямо с телефона!

---

## 📚 Дополнительные ресурсы

- **OpenAPI Specification:** https://swagger.io/specification/
- **Swagger Editor:** https://editor.swagger.io/
- **API Testing.md:** См. файл `API_TESTING.md` для curl примеров

---

## 🎉 Преимущества Swagger

✅ **Интерактивно** - тестируйте API без Postman  
✅ **Документировано** - вся документация в одном месте  
✅ **Актуально** - генерируется из кода  
✅ **Удобно** - красивый UI с автодополнением  
✅ **Sharing** - легко поделиться с командой  

---

**Swagger UI готов к использованию! Откройте http://localhost:3000/api-docs и начните тестировать! 🚀**

