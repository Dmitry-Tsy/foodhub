# 🗺️ Настройка Google Places API для FoodHub

## 📋 Что дает Google Places API

С Google Places API приложение будет:
- ✅ Показывать **реальные рестораны** рядом с пользователем
- ✅ Получать **актуальные данные**: адреса, телефоны, фото
- ✅ Работать **по всему миру**, а не только в Москве
- ✅ Показывать **рейтинги Google** и количество отзывов
- ✅ **Автоматически переключаться** на mock данные если API недоступен

---

## 🔑 Получение API ключа

### Шаг 1: Создайте проект в Google Cloud

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Назовите проект, например "FoodHub"

### Шаг 2: Включите Places API

1. В меню выберите **APIs & Services → Library**
2. Найдите **"Places API"**
3. Нажмите **"Enable"** (Включить)

### Шаг 3: Создайте API ключ

1. Перейдите в **APIs & Services → Credentials**
2. Нажмите **"Create Credentials" → "API Key"**
3. Скопируйте созданный ключ

### Шаг 4: Ограничьте API ключ (рекомендуется)

1. Нажмите на созданный ключ
2. **Application restrictions:**
   - Выберите "Android apps"
   - Добавьте package name: `com.foodhub.app`
   - Добавьте SHA-1 fingerprint вашего keystore

3. **API restrictions:**
   - Выберите "Restrict key"
   - Выберите только "Places API"

4. Сохраните

---

## 📝 Настройка в приложении

### Вариант 1: Через .env файл (рекомендуется)

Создайте файл `.env` в корне проекта:

```env
GOOGLE_PLACES_API_KEY=ВАШ_API_КЛЮЧ_ЗДЕСЬ
```

Установите библиотеку для env:

```bash
npm install react-native-dotenv
npm install --save-dev @types/react-native-dotenv
```

Обновите `babel.config.js`:

```javascript
module.exports = function(api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
    plugins: [
      ['module:react-native-dotenv', {
        moduleName: '@env',
        path: '.env',
      }]
    ],
  };
};
```

### Вариант 2: Прямо в коде (для тестирования)

Откройте `src/services/googlePlacesService.ts` и замените:

```typescript
const GOOGLE_PLACES_API_KEY = 'ВАШ_API_КЛЮЧ_ЗДЕСЬ';
```

⚠️ **Внимание:** Не коммитьте API ключ в git!

---

## 🧪 Тестирование

После настройки API ключа:

1. **Пересоберите приложение:**
   ```bash
   cd android
   ./gradlew assembleRelease
   ```

2. **Установите новый APK**

3. **Откройте экран поиска**

4. **Проверьте консоль:**
   - Должно появиться: `🗺️ Загрузка реальных ресторанов из Google Places API...`
   - При успехе увидите реальные рестораны рядом

---

## 💰 Стоимость Google Places API

**Бесплатно:**
- $200 кредитов каждый месяц
- Nearby Search: $0.032 за запрос
- ~6,250 запросов бесплатно в месяц

**Для разработки:**
- Более чем достаточно для тестирования
- Можно ограничить квоты в Google Cloud Console

---

## 🔄 Как работает fallback

Приложение автоматически определяет доступность Google Places:

```typescript
if (googlePlacesService.isGooglePlacesAvailable()) {
  // Используем Google Places API
  const realRestaurants = await googlePlacesService.searchNearbyRestaurants(...);
} else {
  // Используем mock данные
  return mockRestaurants;
}
```

**Преимущества:**
- ✅ Работает без API ключа (mock данные)
- ✅ Автоматически переключается на реальные данные когда ключ добавлен
- ✅ Graceful degradation при ошибках API

---

## 📊 Что будет показывать Google Places

### Для каждого ресторана:

- **Название** - официальное название из Google
- **Адрес** - точный адрес
- **Координаты** - GPS координаты
- **Фотографии** - до 3 фотографий
- **Рейтинг** - рейтинг Google (0-5) конвертируется в 0-10
- **Количество отзывов** - сколько людей оценили
- **Расстояние** - точное расстояние от вас
- **Тип кухни** - определяется из типов места

### Сортировка:

Рестораны автоматически сортируются **по расстоянию** от вас!

---

## 🐛 Решение проблем

### Ошибка: "REQUEST_DENIED"

API ключ не настроен или недействителен.
- Проверьте что Places API включен
- Проверьте что API ключ правильный

### Ошибка: "OVER_QUERY_LIMIT"

Превышен лимит запросов.
- Проверьте квоты в Google Cloud Console
- Добавьте дебаунсинг для поиска

### Показывает mock данные вместо реальных

1. Проверьте что API ключ установлен
2. Проверьте консоль - должно быть: `🗺️ Загрузка реальных ресторанов...`
3. Проверьте что есть интернет соединение

---

## 🎯 Быстрый старт (минимум действий)

1. **Получите API ключ** (3 минуты)
   - https://console.cloud.google.com/
   - Enable Places API
   - Create API Key

2. **Добавьте в код:**
   ```typescript
   // src/services/googlePlacesService.ts, строка 4
   const GOOGLE_PLACES_API_KEY = 'ВАШ_КЛЮЧ';
   ```

3. **Пересоберите:**
   ```bash
   cd android && ./gradlew assembleRelease
   ```

4. **Готово!** Теперь видите реальные рестораны! 🎉

---

## 📱 Текущий режим

**Без API ключа:**
- Показывает mock рестораны в Москве
- Работает офлайн
- Идеально для демо

**С API ключом:**
- Показывает реальные рестораны рядом с вами
- Требует интернет
- Production ready

---

**Интеграция готова!** Добавьте API ключ когда будет готов. 🗺️

