# 🚀 FoodHub - Быстрый старт

## 📋 Что нужно для запуска

1. **PostgreSQL 15+** установлен и запущен
2. **Node.js 18+** для бэкенда
3. **Android устройство/эмулятор** для APK

---

## 🏃 Запуск за 3 шага

### **Шаг 1: Настройка базы данных**

```bash
# Создать БД
createdb foodhub

# Создать пользователя (если нужно)
psql postgres -c "CREATE USER dmitrytsymlyakov WITH PASSWORD 'foodhub2025';"
psql postgres -c "GRANT ALL PRIVILEGES ON DATABASE foodhub TO dmitrytsymlyakov;"
```

### **Шаг 2: Запуск бэкенда**

```bash
cd backend

# Установить зависимости (первый раз)
npm install

# Запустить сервер
npm run dev
```

Сервер запустится на `http://localhost:3000`

### **Шаг 3: Установка APK**

**Вариант A - Для эмулятора:**
```bash
adb install FoodHub-v3.0-BACKEND.apk
```

Эмулятор будет использовать `http://10.0.2.2:3000` для подключения к бэкенду.

**Вариант B - Для реального устройства:**

1. Узнайте IP вашего компьютера:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```
   
2. Обновите `src/services/api.ts`:
   ```typescript
   const API_BASE_URL = 'http://192.168.x.x:3000/api'; // Ваш IP
   ```

3. Пересоберите APK:
   ```bash
   cd android && ./gradlew assembleRelease
   ```

4. Установите:
   ```bash
   adb install android/app/build/outputs/apk/release/app-release.apk
   ```

---

## ✅ Проверка работы

### 1. **Проверить бэкенд:**
```bash
curl http://localhost:3000/health
```

Ответ:
```json
{
  "status": "ok",
  "timestamp": "2025-11-11T...",
  "uptime": 123.45
}
```

### 2. **Открыть приложение и зарегистрироваться**

1. Запустите FoodHub на устройстве
2. Нажмите "Регистрация"
3. Заполните форму:
   - Username: `testuser`
   - Email: `test@example.com`
   - Password: `password123`
4. Войдите в систему

### 3. **Проверить что данные сохраняются в БД:**
```bash
psql foodhub -c "SELECT * FROM users;"
```

---

## 🔧 Частые проблемы

### ❌ Бэкенд не запускается

**Проблема:** `ECONNREFUSED` или ошибка подключения к PostgreSQL

**Решение:**
1. Проверьте что PostgreSQL запущен:
   ```bash
   pg_ctl status
   ```
2. Проверьте файл `backend/.env`:
   ```env
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=foodhub
   DB_USER=dmitrytsymlyakov
   DB_PASSWORD=foodhub2025
   ```

### ❌ Приложение не подключается к бэкенду

**Проблема:** Network request failed

**Решение для эмулятора:**
- Используйте `10.0.2.2` вместо `localhost`

**Решение для реального устройства:**
- Устройство и компьютер должны быть в одной сети Wi-Fi
- Используйте локальный IP адрес компьютера
- Отключите брандмауэр или разрешите порт 3000

### ❌ JWT ошибка 401

**Проблема:** Unauthorized после регистрации

**Решение:**
1. Убедитесь что `JWT_SECRET` установлен в `.env`
2. Перезапустите бэкенд
3. Удалите приложение и установите заново (очистит токены)

---

## 📚 Полезные команды

### **Бэкенд:**
```bash
npm run dev          # Запуск в dev режиме с hot reload
npm run build        # Компиляция TypeScript
npm start            # Запуск скомпилированного кода
```

### **Проверка БД:**
```bash
psql foodhub                              # Подключиться к БД
\dt                                       # Показать таблицы
SELECT * FROM users;                      # Все пользователи
SELECT * FROM dishes;                     # Все блюда
SELECT * FROM dish_reviews;               # Все отзывы
```

### **APK:**
```bash
cd android
./gradlew assembleRelease                 # Собрать release APK
./gradlew clean                           # Очистить кеш
./gradlew assembleDebug                   # Собрать debug APK
```

---

## 🎯 Следующие шаги

После успешного запуска вы можете:

1. **Добавить ресторан** через поиск
2. **Создать блюдо** в меню ресторана
3. **Оставить отзыв** на блюдо
4. **Настроить вкусовой профиль**
5. **Исследовать AI-функционал**

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте логи бэкенда: `backend/logs/`
2. Проверьте логи APK: `adb logcat | grep FoodHub`
3. Убедитесь что все зависимости установлены

---

**Приятного использования! 🎉**

