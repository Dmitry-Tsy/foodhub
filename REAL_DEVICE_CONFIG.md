# 📱 Конфигурация для реального устройства

## 🌐 Настройки сети

**IP адрес компьютера:** `192.168.31.212`  
**API URL:** `http://192.168.31.212:3000/api`

---

## ✅ Что уже настроено

- ✅ `src/services/api.ts` обновлен с правильным IP
- ✅ APK собран: `FoodHub-v3.0-REAL-DEVICE.apk`
- ✅ Бэкенд готов к работе

---

## 🚀 Инструкция по запуску

### Шаг 1: Подключите устройство и компьютер к одной Wi-Fi сети

Убедитесь что:
- Компьютер подключен к Wi-Fi
- Android устройство подключено к той же Wi-Fi сети
- IP адрес компьютера: `192.168.31.212`

Чтобы проверить IP:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

### Шаг 2: Запустите бэкенд

```bash
cd /Users/dmitrytsymlyakov/AndroidStudioProjects/FoodHub/backend
npm run dev
```

Сервер запустится на `http://192.168.31.212:3000`

### Шаг 3: Проверьте доступность с устройства

**Через браузер на телефоне:**
Откройте `http://192.168.31.212:3000/health`

Должен вернуться JSON:
```json
{
  "status": "ok",
  "timestamp": "...",
  "uptime": 123.45
}
```

### Шаг 4: Установите APK

```bash
adb install FoodHub-v3.0-REAL-DEVICE.apk
```

Или скопируйте APK на устройство и установите вручную.

### Шаг 5: Запустите приложение

1. Откройте FoodHub
2. Нажмите "Регистрация"
3. Создайте аккаунт
4. Начните использовать!

---

## 🔧 Устранение неполадок

### ❌ Проблема: "Network request failed"

**Причина:** Устройство не может подключиться к компьютеру

**Решения:**

1. **Проверьте Wi-Fi:**
   - Устройство и компьютер в одной сети?
   - Проверьте имя сети на обоих устройствах

2. **Проверьте брандмауэр:**
   ```bash
   # macOS - разрешить порт 3000
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/local/bin/node
   ```

3. **Проверьте IP адрес:**
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```
   
   Если IP изменился, обновите `src/services/api.ts` и пересоберите APK.

4. **Проверьте доступность с телефона:**
   - Откройте браузер на телефоне
   - Перейдите на `http://192.168.31.212:3000/health`
   - Если не открывается - проблема в сети

### ❌ Проблема: "ECONNREFUSED"

**Причина:** Бэкенд не запущен

**Решение:**
```bash
cd backend
npm run dev
```

Проверьте что в консоли появилось:
```
🚀 Server running on port 3000
```

### ❌ Проблема: "Unauthorized" (401)

**Причина:** Проблема с JWT токеном

**Решение:**
1. Удалите приложение полностью
2. Переустановите APK
3. Зарегистрируйтесь заново

### ❌ IP адрес изменился

**Решение:**

1. Узнайте новый IP:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```

2. Обновите `src/services/api.ts`:
   ```typescript
   const API_BASE_URL = 'http://НОВЫЙ_IP:3000/api';
   ```

3. Пересоберите:
   ```bash
   cd android
   ./gradlew assembleRelease
   ```

4. Переустановите APK

---

## 📋 Чеклист перед использованием

- [ ] Устройство и компьютер в одной Wi-Fi сети
- [ ] Бэкенд запущен (`npm run dev`)
- [ ] Health check работает (`http://192.168.31.212:3000/health`)
- [ ] APK установлен
- [ ] Брандмауэр разрешает соединения на порт 3000

---

## 🔄 Переключение между эмулятором и реальным устройством

### Для эмулятора:
```typescript
// src/services/api.ts
const API_BASE_URL = 'http://10.0.2.2:3000/api';
```

### Для реального устройства:
```typescript
// src/services/api.ts
const API_BASE_URL = 'http://192.168.31.212:3000/api';
```

После изменения всегда пересобирайте APK!

---

## 💡 Полезные команды

```bash
# Узнать IP компьютера
ifconfig | grep "inet " | grep -v 127.0.0.1

# Запустить бэкенд
cd backend && npm run dev

# Собрать APK
cd android && ./gradlew assembleRelease

# Установить APK
adb install FoodHub-v3.0-REAL-DEVICE.apk

# Проверить health check
curl http://192.168.31.212:3000/health

# Логи с устройства
adb logcat | grep FoodHub
```

---

**Готово! Приложение настроено для работы на реальном устройстве! 🎉**

