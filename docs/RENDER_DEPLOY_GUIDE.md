# 🚀 Деплой FoodHub на Render.com

## 📋 Что такое Render?

**Render.com** - современный бесплатный хостинг для web-приложений с автоматическим деплоем.

### ✅ Преимущества:
- 🆓 **Бесплатный tier** (750 часов в месяц)
- 🗄️ **Бесплатная PostgreSQL** (до 1 GB)
- 🚀 **Автоматический деплой** из Git
- 🔒 **HTTPS из коробки**
- 🌍 **Глобальный доступ** (любое устройство может подключиться)

### ⚠️ Ограничения бесплатного tier:
- "Засыпает" после 15 минут неактивности (первый запрос будет медленным)
- 512 MB RAM
- 0.1 CPU

**Для разработки и тестирования - идеально! 👌**

---

## 🎯 Пошаговая инструкция

### Шаг 1: Регистрация на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Get Started"**
3. Зарегистрируйтесь через:
   - GitHub (рекомендуется)
   - GitLab
   - Email

---

### Шаг 2: Подготовка кода (УЖЕ СДЕЛАНО ✅)

Ваш бэкенд уже готов к деплою! Все необходимые файлы созданы:

- ✅ `render.yaml` - конфигурация для Render
- ✅ `backend/src/config/database.ts` - поддержка DATABASE_URL
- ✅ `.gitignore` - игнорирование .env файлов
- ✅ `package.json` - scripts для build и start

---

### Шаг 3: Загрузка кода на GitHub

#### Вариант A: Если у вас еще НЕТ Git репозитория

```bash
# 1. Перейдите в директорию проекта
cd /Users/dmitrytsymlyakov/AndroidStudioProjects/FoodHub

# 2. Инициализируйте Git
git init

# 3. Добавьте все файлы
git add .

# 4. Сделайте первый коммит
git commit -m "Initial commit - FoodHub backend ready for deployment"

# 5. Создайте репозиторий на GitHub:
#    - Перейдите на github.com
#    - Нажмите "+" → "New repository"
#    - Назовите: "foodhub"
#    - НЕ добавляйте README/gitignore (уже есть)
#    - Создайте

# 6. Подключите remote и запушьте
git remote add origin https://github.com/ВАШ_USERNAME/foodhub.git
git branch -M main
git push -u origin main
```

#### Вариант B: Если Git репозиторий УЖЕ ЕСТЬ

```bash
cd /Users/dmitrytsymlyakov/AndroidStudioProjects/FoodHub

# Убедитесь что все изменения закоммичены
git add .
git commit -m "Add Render deployment configuration"
git push
```

---

### Шаг 4: Создание сервиса на Render

#### 🗄️ A. Создайте PostgreSQL базу данных

1. В Render Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Заполните:
   - **Name:** `foodhub-db`
   - **Database:** `foodhub`
   - **User:** `foodhub_user`
   - **Region:** Oregon (или ближайший к вам)
   - **Plan:** **Free**
3. Нажмите **"Create Database"**
4. ⏳ Подождите 2-3 минуты (создается база)
5. ✅ База готова! Скопируйте **Internal Database URL** (понадобится)

#### 🌐 B. Создайте Web Service (Backend API)

1. Вернитесь в Dashboard
2. Нажмите **"New +"** → **"Web Service"**
3. Выберите **"Build and deploy from a Git repository"**
4. Подключите ваш GitHub репозиторий:
   - Если первый раз - нажмите **"Connect GitHub"**
   - Выберите репозиторий `foodhub`
5. Заполните настройки:

   ```
   Name: foodhub-backend
   Region: Oregon (тот же что и база!)
   Branch: main
   Root Directory: backend
   Environment: Node
   Build Command: npm install && npm run build
   Start Command: npm start
   Plan: Free
   ```

6. **Environment Variables** (переменные окружения):

   Нажмите **"Add Environment Variable"** и добавьте:

   | Key | Value |
   |-----|-------|
   | `NODE_ENV` | `production` |
   | `PORT` | `3000` |
   | `JWT_SECRET` | `your-super-secret-key-change-this` |
   | `DATABASE_URL` | `Internal Database URL из шага A` |
   | `ALLOWED_ORIGINS` | `*` |

   **ВАЖНО:** 
   - `DATABASE_URL` должен быть Internal URL из PostgreSQL
   - `JWT_SECRET` - придумайте свой секретный ключ!

7. Нажмите **"Create Web Service"**

8. ⏳ **Деплой начался!** Это займет 3-5 минут:
   - Installing dependencies...
   - Building TypeScript...
   - Starting server...

9. ✅ **Деплой завершен!** Вы увидите:
   ```
   ✅ PostgreSQL connected successfully
   📊 Database models synchronized (production mode)
   🚀 Server running on port 3000
   ```

---

### Шаг 5: Получите URL вашего API

После успешного деплоя вы увидите URL вашего бэкенда:

```
https://foodhub-backend.onrender.com
```

**Проверьте что работает:**

1. Откройте в браузере:
   ```
   https://foodhub-backend.onrender.com/health
   ```

   Должно показать:
   ```json
   {
     "status": "ok",
     "timestamp": "2025-11-13T...",
     "uptime": 123.45
   }
   ```

2. Swagger UI:
   ```
   https://foodhub-backend.onrender.com/api-docs
   ```

✅ **API работает и доступен из любой точки мира!**

---

## 🎨 Обновление Frontend

Теперь нужно обновить мобильное приложение, чтобы оно использовало production API вместо localhost.

### В файле `src/services/api.ts`:

Замените:
```typescript
const API_BASE_URL = 'http://10.0.2.2:3000/api';
```

На:
```typescript
const API_BASE_URL = 'https://foodhub-backend.onrender.com/api';
```

### Соберите новый APK:

```bash
cd /Users/dmitrytsymlyakov/AndroidStudioProjects/FoodHub/android
export ANDROID_HOME=$HOME/Library/Android/sdk
./gradlew assembleRelease

# Скопируйте APK
cd ..
cp android/app/build/outputs/apk/release/app-release.apk FoodHub-PRODUCTION.apk
```

### Установите на устройство:

```bash
adb install FoodHub-PRODUCTION.apk
```

✅ **Теперь приложение работает с production API!**

---

## 🔄 Автоматический деплой

**Render автоматически обновляет сервис при каждом push в GitHub!**

```bash
# Внесите изменения в код
vim backend/src/routes/auth.ts

# Закоммитьте и запушьте
git add .
git commit -m "Update auth logic"
git push

# 🚀 Render автоматически:
# 1. Заметит новый коммит
# 2. Запустит build
# 3. Задеплоит новую версию
# 4. Все это за 2-3 минуты!
```

---

## 🧪 Тестирование API

### 1. Health Check
```bash
curl https://foodhub-backend.onrender.com/health
```

### 2. Регистрация пользователя
```bash
curl -X POST https://foodhub-backend.onrender.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "password123"
  }'
```

### 3. Логин
```bash
curl -X POST https://foodhub-backend.onrender.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'
```

---

## ⚙️ Управление сервисом

### Логи сервера

В Render Dashboard:
1. Откройте ваш Web Service
2. Перейдите на вкладку **"Logs"**
3. Смотрите real-time логи!

### Перезапуск сервиса

1. В Dashboard откройте сервис
2. Нажмите **"Manual Deploy"** → **"Clear build cache & deploy"**

### Environment Variables

1. В Dashboard откройте сервис
2. Перейдите на **"Environment"**
3. Добавляйте/изменяйте переменные
4. Нажмите **"Save Changes"**

---

## 🐛 Troubleshooting

### ❌ "Build failed"

**Проблема:** Ошибка при установке зависимостей

**Решение:**
1. Проверьте что `backend/package.json` корректен
2. Убедитесь что Root Directory = `backend`
3. Попробуйте **"Clear build cache & deploy"**

### ❌ "Database connection failed"

**Проблема:** Не удается подключиться к БД

**Решение:**
1. Проверьте что `DATABASE_URL` правильный (Internal URL!)
2. Убедитесь что база и сервис в одном регионе
3. Проверьте что база запущена (в Dashboard)

### ❌ "Service unavailable after 15 minutes"

**Проблема:** Это нормально! Бесплатный tier "засыпает"

**Решение:**
- Первый запрос после сна = 30-60 секунд (холодный старт)
- Последующие запросы = быстро
- Для production рассмотрите платный план ($7/месяц = без сна)

---

## 💰 Стоимость

### Бесплатный tier:

| Ресурс | Лимит | Хватит? |
|--------|-------|---------|
| Web Service | 750 часов/месяц | ✅ Да |
| PostgreSQL | 1 GB | ✅ Да (для начала) |
| RAM | 512 MB | ✅ Да |
| Bandwidth | 100 GB/месяц | ✅ Да |

**Для разработки и тестирования - более чем достаточно!**

### Платные планы (опционально):

- **Starter ($7/мес):** Без сна, больше RAM
- **Standard ($25/мес):** Еще больше ресурсов

---

## 🎯 Следующие шаги

После успешного деплоя:

1. ✅ **Протестируйте API** через Swagger
2. ✅ **Обновите мобильное приложение** с production URL
3. ✅ **Создайте тестового пользователя**
4. ✅ **Добавьте несколько ресторанов и блюд**
5. ✅ **Поделитесь APK** с друзьями для тестирования!

---

## 📞 Поддержка

### Полезные ссылки:

- 📚 [Render Documentation](https://render.com/docs)
- 💬 [Render Community](https://community.render.com)
- 🐛 [GitHub Issues](https://github.com/YOUR_USERNAME/foodhub/issues)

### Если что-то не работает:

1. Проверьте логи в Render Dashboard
2. Убедитесь что все environment variables установлены
3. Попробуйте перезапустить сервис
4. Проверьте что база данных запущена

---

## 🎊 Поздравляем!

Ваш бэкенд теперь в облаке и доступен 24/7! 🚀

**URL вашего API:**
```
https://foodhub-backend.onrender.com/api
```

**Swagger UI:**
```
https://foodhub-backend.onrender.com/api-docs
```

Теперь ваше приложение может работать на любом устройстве без необходимости запускать локальный сервер! 🎉

---

## 🔐 Важные напоминания

1. ⚠️ **Измените JWT_SECRET** на случайную строку
2. ⚠️ **Не коммитьте .env файлы** в Git (уже в .gitignore)
3. ⚠️ **Регулярно делайте backup БД** (в Render Dashboard)
4. ✅ **Используйте HTTPS** (автоматически включен)

---

**Готово! Ваш FoodHub теперь в облаке!** ☁️🚀

