# Источники данных для поиска ресторанов

В приложении FoodHub реализована поддержка нескольких источников данных для поиска ресторанов. Система автоматически выбирает источник по приоритету.

## Доступные источники

### 1. OpenStreetMap (OSM) ⭐ РЕКОМЕНДУЕТСЯ

**Статус:** ✅ Полностью бесплатный, без ограничений  
**Приоритет:** Высокий (первый в списке)

**Преимущества:**
- Полностью бесплатный
- Нет лимитов на запросы
- Хорошее покрытие по всему миру
- Открытые данные, поддерживаемые сообществом

**Недостатки:**
- Может быть менее точным чем коммерческие API
- Меньше фотографий и детальной информации
- Запросы могут быть медленнее

**Настройка:**
Не требует настройки, работает сразу.

**Использование:**
```typescript
import * as osmService from './services/openStreetMapService';

const restaurants = await osmService.searchNearbyRestaurantsOSM(
  location,
  radius,
  maxResults
);
```

---

### 2. Foursquare Places API

**Статус:** ✅ Бесплатный tier (50,000 запросов/день)  
**Приоритет:** Средний

**Преимущества:**
- Хорошая база данных ресторанов
- Детальная информация (фото, рейтинги, часы работы)
- API ключ получается быстро
- 50,000 запросов в день бесплатно

**Недостатки:**
- Требует регистрации и API ключа
- Есть лимиты на бесплатном плане

**Настройка:**
1. Зарегистрируйтесь на https://developer.foursquare.com/
2. Создайте приложение
3. Получите API Key
4. Установите переменные окружения:
   ```bash
   FOURSQUARE_API_KEY=your_api_key_here
   ```

**Использование:**
```typescript
import * as foursquareService from './services/foursquareService';

const restaurants = await foursquareService.searchNearbyRestaurantsFoursquare(
  location,
  radius,
  maxResults
);
```

---

### 3. Google Places API

**Статус:** 💰 Платный (после бесплатного лимита $200/месяц)  
**Приоритет:** Низкий (используется как fallback)

**Преимущества:**
- Самая точная и полная база данных
- Много фотографий
- Детальная информация

**Недостатки:**
- Дорогой после бесплатного лимита
- Требует настройки billing

**Настройка:**
1. Создайте проект в Google Cloud Console
2. Включите Places API
3. Создайте API ключ
4. Установите переменные окружения:
   ```bash
   GOOGLE_PLACES_API_KEY=your_api_key_here
   ```

---

### 4. Yelp Fusion API (опционально)

**Статус:** ✅ Бесплатный tier (5,000 запросов/день)

**Преимущества:**
- Хорошая база данных с отзывами
- Детальная информация о ресторанах

**Недостатки:**
- Меньше запросов чем Foursquare (5,000/день)
- Преимущественно для США и некоторых других стран

**Настройка:**
1. Зарегистрируйтесь на https://www.yelp.com/developers
2. Создайте приложение
3. Получите API Key
4. Добавьте сервис в `restaurantService.ts`

---

### 5. Mock данные

**Статус:** ✅ Всегда доступен  
**Приоритет:** Последний (fallback)

Используется когда все внешние источники недоступны.

---

## Настройка приоритета источников

По умолчанию используется приоритет: `osm,foursquare,google,mock`

Для изменения приоритета установите переменную окружения:

```bash
RESTAURANT_DATA_SOURCE=osm,foursquare,google,mock
```

Или для использования только OSM:

```bash
RESTAURANT_DATA_SOURCE=osm,mock
```

Или только Foursquare:

```bash
RESTAURANT_DATA_SOURCE=foursquare,osm,mock
```

---

## Рекомендации

1. **Для production с бюджетом 0₽:** Используйте OpenStreetMap как основной источник
2. **Для production с небольшим бюджетом:** Foursquare (50k запросов/день бесплатно)
3. **Для максимального качества:** Google Places API (но учтите стоимость)

---

## Пример настройки для разработки

В `.env` или в настройках приложения:

```env
# Отключить Google Places (экономия средств)
GOOGLE_PLACES_API_KEY=

# Включить Foursquare (если есть ключ)
FOURSQUARE_API_KEY=your_key_here
FOURSQUARE_API_SECRET=your_secret_here

# Приоритет источников
RESTAURANT_DATA_SOURCE=osm,foursquare,mock
```

---

## Интеграция собственной базы данных

Лучший долгосрочный подход - собирать рестораны через пользователей:

1. Пользователи добавляют рестораны в приложение
2. Данные сохраняются в вашей базе данных
3. Поиск выполняется по собственной БД + внешние источники как дополнение

Это уже частично реализовано в приложении через `getOrCreateRestaurantInDB`.

