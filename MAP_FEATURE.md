# 🗺️ Функционал Карты - FoodHub v1.3.0

## ✨ Что добавлено

### 1. Интерактивная карта ресторанов
- **Компонент:** `RestaurantMap.tsx`
- **Технология:** Google Maps JavaScript API через WebView
- **Преимущество:** Без проблем совместимости с React Native версиями

### 2. Возможности карты

#### 📍 Отображение маркеров
- Каждый ресторан отмечен на карте с номером
- Маркеры кликабельны - открывают детальную страницу ресторана
- Автоматическое центрирование по всем маркерам

#### 🔵 Ваше местоположение
- Показывается синей точкой на карте
- Автоматически центрируется при первом открытии

#### 🎨 UI элементы
- **Кнопка переключения** (список ↔ карта) в правом верхнем углу поиска
- **Счетчик ресторанов** - показывает количество на карте
- **Легенда** - объясняет символы (Вы + Рестораны)
- **Кнопка закрытия** - для модального режима (если понадобится)

### 3. Интеграция в SearchScreen

```typescript
// Кнопка переключения между списком и картой
<TouchableOpacity
  style={[styles.filterButton, showMap && styles.filterButtonActive]}
  onPress={() => setShowMap(!showMap)}
>
  <Ionicons
    name={showMap ? 'list' : 'map'}
    size={24}
    color={showMap ? Theme.colors.primary : Theme.colors.text}
  />
</TouchableOpacity>

// Условный рендеринг
{showMap ? (
  <RestaurantMap
    restaurants={restaurants}
    userLocation={userLocation}
    onRestaurantPress={handleRestaurantPress}
  />
) : (
  <FlatList ... />
)}
```

## 🔧 Технические детали

### Использованные библиотеки
- `react-native-webview` - для отображения Google Maps
- Google Maps JavaScript API - для рендеринга карты
- Тот же API ключ что и для Places API

### Архитектура компонента

#### HTML генерация
```typescript
const generateMapHTML = () => {
  // Создает полный HTML документ с Google Maps
  // Включает маркеры для всех ресторанов
  // Настраивает обработчики событий
}
```

#### Коммуникация с React Native
```typescript
// Из WebView в React Native
window.ReactNativeWebView.postMessage(JSON.stringify({
  type: 'restaurantClick',
  restaurantId: '...'
}));

// В React Native
const handleMessage = (event) => {
  const data = JSON.parse(event.nativeEvent.data);
  if (data.type === 'restaurantClick') {
    onRestaurantPress(data.restaurantId);
  }
};
```

### Стили и UI

#### Positioning
- Счетчик: `top: 16, right: 16`
- Легенда: `bottom: 16, left: 16`
- Кнопка закрытия: `top: 16, left: 16`

#### Цвета
- Маркеры ресторанов: Красные с номерами (Google Maps default)
- Ваша позиция: Синяя точка (`#4285F4`)
- Активная кнопка: `${Theme.colors.primary}20` фон + `Theme.colors.primary` border

## 📱 Использование

### Для пользователя

1. **Открыть карту:**
   - Перейдите на вкладку "Поиск" 🔍
   - Нажмите иконку карты в правом верхнем углу

2. **Взаимодействие:**
   - Карта показывает все найденные рестораны
   - Синяя точка - ваше местоположение
   - Нажмите на маркер ресторана → откроется детальная страница

3. **Вернуться к списку:**
   - Нажмите иконку списка (там где была карта)

### Для разработчика

#### Добавление карты в другие экраны

```typescript
import { RestaurantMap } from '../components';

// В компоненте
<RestaurantMap
  restaurants={restaurants}           // массив Restaurant[]
  userLocation={userLocation}         // { latitude, longitude } | null
  onRestaurantPress={handlePress}     // (id: string) => void
  onClose={handleClose}               // optional, для модального режима
/>
```

#### Кастомизация

**Изменить zoom:**
```typescript
// В generateMapHTML()
zoom: 13,  // Измените на 10-20
```

**Изменить стиль маркеров:**
```typescript
// В generateMapHTML()
label: "${index + 1}",  // Показывает номер
// Или уберите label для простых маркеров
```

**Изменить центр по умолчанию:**
```typescript
return { latitude: 55.7558, longitude: 37.6173 }; // Москва
// Измените на ваш город
```

## 🔑 Google Maps API

### Использованный API ключ
```
AIzaSyCnveR2zXFc-UMPCvhD49A51ayEHG99W98
```

### Необходимые API
- ✅ Google Maps JavaScript API
- ✅ Google Places API (для поиска ресторанов)

### Квоты и лимиты
- Maps JavaScript API: 28,000 загрузок карты бесплатно/месяц
- После лимита: $7 за 1000 загрузок

## 🐛 Troubleshooting

### Карта не загружается
1. Проверьте интернет соединение
2. Проверьте что API ключ активен в Google Cloud Console
3. Проверьте логи: `adb logcat | grep "ReactNativeJS"`

### Маркеры не кликабельны
- Убедитесь что `onMessage` prop передан в `<WebView>`
- Проверьте что `onRestaurantPress` функция определена

### Карта показывает неправильное местоположение
```typescript
// В getCenterLocation() измените fallback:
return { latitude: YOUR_LAT, longitude: YOUR_LNG };
```

## 📊 Производительность

### Оптимизации
- ✅ HTML генерируется один раз при рендере
- ✅ Используется `StyleSheet.absoluteFillObject` для оптимального layout
- ✅ Минимальное количество re-renders

### Рекомендации
- До 50 маркеров: отличная производительность
- 50-100 маркеров: хорошая производительность
- Больше 100: рассмотрите кластеризацию маркеров

## 🚀 Будущие улучшения

### Планируемые фичи
- [ ] Кластеризация маркеров для большого количества ресторанов
- [ ] Фильтры на карте (кухня, рейтинг)
- [ ] Routing до ресторана
- [ ] Переключение стилей карты (стандартная/спутник)
- [ ] Поиск по карте при перемещении
- [ ] Сохранение позиции карты между переключениями

## 📦 Версии и зависимости

```json
{
  "react-native-webview": "^13.6.4",
  "react-native": "0.73.6",
  "expo": "~50.0.21"
}
```

## ✅ Чеклист интеграции

- [x] Установить `react-native-webview`
- [x] Создать компонент `RestaurantMap`
- [x] Добавить в `components/index.ts`
- [x] Интегрировать в `SearchScreen`
- [x] Добавить кнопку переключения
- [x] Настроить Google Maps API ключ
- [x] Протестировать на реальном устройстве
- [x] Собрать APK
- [x] Документировать

---

**Версия:** 1.3.0  
**Дата:** 11 ноября 2024  
**Статус:** ✅ Готово к использованию

