# 📦 Инструкция по сборке APK для FoodHub

## Способ 1: EAS Build (Рекомендуется) 🚀

### Шаг 1: Установите EAS CLI

```bash
npm install -g eas-cli
```

### Шаг 2: Войдите в Expo аккаунт

```bash
eas login
```

Если у вас нет аккаунта:
```bash
# Зарегистрируйтесь на https://expo.dev
# Или используйте команду
eas register
```

### Шаг 3: Настройте проект

```bash
eas build:configure
```

### Шаг 4: Соберите APK

```bash
# Для preview версии (быстрая сборка)
eas build --platform android --profile preview

# Для production версии
eas build --platform android --profile production
```

### Шаг 5: Скачайте APK

После завершения сборки (5-15 минут):
- Откройте ссылку, которую покажет команда
- Или зайдите на https://expo.dev
- Скачайте готовый APK файл

---

## Способ 2: Локальная сборка с EAS Build 💻

Если хотите собрать APK локально на вашем компьютере:

### Требования:
- Android Studio
- Android SDK
- JDK 17

### Команда:

```bash
eas build --platform android --profile preview --local
```

**Преимущества:**
- ✅ Быстрее (нет загрузки в облако)
- ✅ Работает без интернета
- ✅ Бесплатно (без лимитов)

**Недостатки:**
- ❌ Требует Android SDK
- ❌ Занимает место на диске

---

## Способ 3: Expo Go (только для тестирования) 📱

Если нужно просто протестировать на устройстве БЕЗ сборки APK:

```bash
npm start
```

Затем отсканируйте QR-код в приложении Expo Go.

**Внимание:** Это НЕ создает APK, только для разработки!

---

## Способ 4: Классический Expo Build (Устаревший) ⚠️

```bash
expo build:android
```

⚠️ **Не рекомендуется** - старый метод, будет удален в будущих версиях Expo.

---

## 🎯 Рекомендуемый процесс для FoodHub

### Для тестирования (быстро):

```bash
# 1. Установите EAS CLI
npm install -g eas-cli

# 2. Войдите
eas login

# 3. Соберите preview APK
eas build --platform android --profile preview

# 4. Дождитесь завершения и скачайте APK
```

### Для production (для публикации):

```bash
# 1. Обновите версию в app.json
# "version": "1.0.0" -> "1.0.1"

# 2. Соберите production build
eas build --platform android --profile production

# 3. Тестируйте APK
# 4. Публикуйте в Google Play Store (опционально)
```

---

## 📋 Конфигурация (eas.json)

Уже создан файл `eas.json` с настройками:

- **development** - для разработки с Dev Client
- **preview** - быстрая сборка APK для тестирования
- **production** - финальная версия для публикации

---

## 🔧 Настройка app.json перед сборкой

Убедитесь, что в `app.json` указаны:

```json
{
  "expo": {
    "name": "FoodHub",
    "slug": "foodhub",
    "version": "1.0.0",
    "android": {
      "package": "com.foodhub.app",
      "versionCode": 1
    }
  }
}
```

**Важно:** 
- `package` должен быть уникальным (например, com.yourcompany.foodhub)
- `versionCode` увеличивайте при каждой новой сборке

---

## ⏱ Время сборки

- **EAS Build (облако):** 5-15 минут
- **EAS Build (локально):** 2-5 минут (первый раз дольше)
- **Expo Go:** 0 минут (но это не APK)

---

## 💰 Стоимость

**EAS Build:**
- 🆓 Free tier: 30 билдов/месяц
- 💎 Premium: безлимит

**Expo Go:**
- 🆓 Полностью бесплатно

---

## 🐛 Решение проблем

### Ошибка: "No account found"

```bash
eas login
```

### Ошибка: "Android SDK not found" (при локальной сборке)

Установите Android Studio и настройте переменные окружения:

```bash
export ANDROID_HOME=$HOME/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
```

### Ошибка: "Build failed"

Проверьте логи:
```bash
eas build:list
```

Откройте детали билда и посмотрите ошибки.

---

## 📱 Установка APK на устройство

После получения APK:

### Способ 1: Прямая установка
1. Скопируйте APK на телефон
2. Откройте файл
3. Разрешите установку из неизвестных источников
4. Установите

### Способ 2: ADB
```bash
adb install path/to/app.apk
```

### Способ 3: Ссылка
Expo создаст ссылку, по которой можно скачать APK напрямую на устройство.

---

## 🎉 Готово!

После сборки у вас будет:
- ✅ Полнофункциональный APK файл
- ✅ Работает без Expo Go
- ✅ Можно делиться с другими
- ✅ Можно публиковать в Google Play

---

## 📚 Дополнительные ресурсы

- [EAS Build документация](https://docs.expo.dev/build/introduction/)
- [Android Setup](https://docs.expo.dev/build-reference/android-builds/)
- [Google Play публикация](https://docs.expo.dev/submit/android/)

---

**Удачной сборки!** 🚀📱

