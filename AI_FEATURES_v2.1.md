# 🤖 AI Features & Gamification - v2.1

## ✅ Реализовано

### 1. 🎯 Вкусовой профиль (`TasteProfileScreen`)

**Функционал:**
- ✅ Выбор любимых кухонь (Итальянская, Японская, и т.д.)
- ✅ Выбор любимых ингредиентов (Морепродукты, Грибы, и т.д.)
- ✅ Указание аллергий и исключений (Арахис, Глютен, и т.д.)
- ✅ Установка уровня остроты (от "Не острое" до "Экстрим" 🔥)
- ✅ Сохранение профиля

**Как использовать:**
```typescript
// Перейти на экран
navigation.navigate('TasteProfile');

// После заполнения, система:
// - Фильтрует блюда с аллергенами
// - Подбирает рекомендации
// - Учитывает предпочтения
```

**UI:**
- Чипы для выбора (тап = выбор/снятие)
- Красные чипы для аллергий
- Эмодзи для уровней остроты (😋 → 🔥)

---

### 2. 🏆 Система достижений (`achievementService.ts`)

**Достижения:**

| ID | Название | Условие | Награда |
|----|----------|---------|---------|
| `first_step` | Первые шаги | 1 отзыв | 10 points, 🎯 |
| `food_critic` | Гастрономический критик | 30 отзывов | 100 points, 🍽️ |
| `adventurer` | Искатель приключений | 10 кухонь | 150 points, 🗺️ |
| `explorer_5` | Путешественник | 5 кухонь | 50 points, ✈️ |
| `photographer` | Фуд-фотограф | 50 фото | 75 points, 📸 |
| `menu_builder` | Строитель меню | 20 блюд добавлено | 120 points, 🍴 |
| `trusted_expert` | Доверенный эксперт | Trust score > 4.5 | 200 points, ⭐ |
| `influencer` | Гастро-инфлюенсер | 100 подписчиков | 250 points, 👑 |

**API:**
```typescript
import { checkAchievements, getUserAchievements } from '../services/achievementService';

// Проверка достижений
const newAchievements = await checkAchievements(userId, {
  reviewsCount: 30,
  cuisinesTried: 10,
  photosUploaded: 50,
  trustScore: 4.8,
  dishesAdded: 20,
  followersCount: 100,
});

// Получение достижений пользователя
const achievements = await getUserAchievements(userId);
```

**Компонент:**
```tsx
<AchievementBadge
  achievement={achievement}
  userAchievement={userAchievement}
  size="medium" // small | medium | large
/>
```

**Визуал:**
- Разблокированные: золотая иконка + эмодзи бейдж
- Заблокированные: серые, полупрозрачные
- Прогресс-бар снизу (если начато, но не завершено)

---

### 3. 🍷 AI-сомелье (`recommendationService.ts`)

**Функционал:**
- Анализирует описание блюда
- Подбирает идеальные напитки
- Объясняет сочетания

**Правила подбора:**

| Блюдо | Напиток | Причина |
|-------|---------|---------|
| Стейк, говядина | Каберне, Мальбек | Танины дополняют жирное мясо |
| Рыба, морепродукты | Шардоне, Совиньон Блан | Легкость подчеркивает деликатность |
| Паста | Кьянти, Пино Гриджио | Классика итальянской кухни |
| Суши, роллы | Саке, Рислинг | Создан для японской кухни |
| Рамен | Японское пиво, зеленый чай | Освежает между ложками |
| Острое | Рислинг (сладкий), IPA | Балансирует остроту |
| Десерт | Портвейн, эспрессо | Дополняет или контрастирует |

**API:**
```typescript
import { suggestPairings } from '../services/recommendationService';

const pairings = await suggestPairings(dish);
// Возвращает: Pairing[]
```

**Компонент:**
```tsx
<PairingCard
  pairing={pairing}
  onVote={(positive) => console.log('User voted:', positive)}
/>
```

**Визуал:**
- Иконка напитка (🍷🍺🍸🍶☕💧)
- % совпадения (цветной индикатор)
- AI бейдж (✨ AI)
- Объяснение
- Кнопки голосования (👍 / 👎)

---

### 4. 🔍 Поиск по ингредиентам

**API:**
```typescript
import { searchByIngredients } from '../services/recommendationService';

const dishes = await searchByIngredients(
  ['Морепродукты', 'Рис'],  // Должно содержать
  ['Глютен', 'Арахис']      // Исключить
);
```

**Алгоритм:**
- Поиск по описанию блюда (lowercase)
- Фильтрация по наличию требуемых ингредиентов
- Исключение блюд с аллергенами

---

### 5. 🎁 Рекомендации (`recommendationService.ts`)

**Алгоритм:**
```typescript
import { generateRecommendations } from '../services/recommendationService';

const recommendations = await generateRecommendations(
  tasteProfile,  // Вкусовой профиль
  allDishes,     // Все блюда
  allRestaurants // Все рестораны
);
```

**Скоринг:**
- Любимая кухня: +15 points
- Любимый ингредиент: +15 points каждый
- Исключенный ингредиент: блюдо отбрасывается
- Ценовой диапазон: +10/-20 points
- Высокий рейтинг (>8): +15 points

**Результат:**
```typescript
interface Recommendation {
  type: 'dish' | 'restaurant' | 'cuisine';
  itemId: string;
  score: number; // 0-100
  reason: string; // "Вам понравится, потому что..."
  tags: string[];
}
```

---

## 🎨 UI компоненты

### 1. `AchievementBadge` ✅

**Props:**
```typescript
{
  achievement: Achievement;
  userAchievement?: UserAchievement;
  size?: 'small' | 'medium' | 'large';
}
```

**Состояния:**
- 🔒 Заблокировано: серая иконка, opacity 0.5
- 📊 В процессе: прогресс-бар снизу
- ✅ Разблокировано: золотая иконка + эмодзи

### 2. `PairingCard` ✅

**Props:**
```typescript
{
  pairing: Pairing;
  onVote?: (positive: boolean) => void;
}
```

**Элементы:**
- Иконка напитка
- Название напитка
- % совпадения (цветной)
- AI бейдж
- Объяснение
- Кнопки голосования

### 3. `TasteProfileScreen` ✅

**Секции:**
1. Любимые кухни (14 вариантов)
2. Любимые ингредиенты (15 вариантов)
3. Аллергии (10 частых аллергенов)
4. Уровень остроты (5 уровней с эмодзи)

---

## 📱 Интеграция с экранами

### Где добавить AI-сомелье:

**DishDetailScreen:**
```tsx
import { PairingCard } from '../components';
import { suggestPairings } from '../services/recommendationService';

// В компоненте
const [pairings, setPairings] = useState<Pairing[]>([]);

useEffect(() => {
  if (currentDish) {
    suggestPairings(currentDish).then(setPairings);
  }
}, [currentDish]);

// В render
{pairings.length > 0 && (
  <View>
    <Text style={styles.sectionTitle}>🍷 Что к этому подойдет</Text>
    {pairings.map(p => (
      <PairingCard key={p.drink} pairing={p} onVote={handleVote} />
    ))}
  </View>
)}
```

### Где добавить достижения:

**ProfileScreen:**
```tsx
import { AchievementBadge } from '../components';
import { getUserAchievements, ACHIEVEMENTS } from '../services/achievementService';

// В компоненте
const [userAchievements, setUserAchievements] = useState([]);

useEffect(() => {
  getUserAchievements(userId).then(setUserAchievements);
}, []);

// В render
<View style={styles.achievementsSection}>
  <Text style={styles.sectionTitle}>🏆 Достижения</Text>
  <ScrollView horizontal>
    {ACHIEVEMENTS.map(achievement => (
      <AchievementBadge
        key={achievement.id}
        achievement={achievement}
        userAchievement={userAchievements.find(ua => ua.achievementId === achievement.id)}
      />
    ))}
  </ScrollView>
</View>
```

### Где добавить вкусовой профиль:

**ProfileScreen (кнопка):**
```tsx
<Button
  title="⚙️ Настроить вкусовой профиль"
  onPress={() => navigation.navigate('TasteProfile')}
  variant="outline"
/>
```

---

## 🚀 Следующие шаги для полной интеграции

### Шаг 1: Обновить типы навигации
```typescript
// src/types/index.ts
export type RootStackParamList = {
  // ... existing
  TasteProfile: undefined;
  Achievements: undefined;
};
```

### Шаг 2: Добавить экраны в навигатор
```typescript
// src/navigation/AppNavigator.tsx
<Stack.Screen 
  name="TasteProfile" 
  component={TasteProfileScreen}
  options={{ title: 'Вкусовой профиль' }}
/>
```

### Шаг 3: Интегрировать AI-сомелье
```typescript
// В DishDetailScreen добавить секцию с PairingCard
```

### Шаг 4: Добавить достижения в ProfileScreen
```typescript
// Горизонтальный скролл с AchievementBadge
```

### Шаг 5: Добавить проверку достижений
```typescript
// После добавления отзыва/блюда/фото
checkAchievements(userId, stats).then(newAchievements => {
  if (newAchievements.length > 0) {
    // Показать модалку "Новое достижение разблокировано!"
  }
});
```

---

## 📊 Метрики и аналитика

**Что отслеживать:**
- Количество настроенных профилей
- Использование AI-сомелье (% кликов)
- Разблокированные достижения
- Время до первого достижения
- Конверсия guest → registered (благодаря геймификации)

---

## 🎮 Геймификация (Roadmap v2.2+)

### Планируется:

**Челленджи:**
```typescript
interface Challenge {
  title: "Попробуй 5 блюд тайской кухни";
  goal: { target: 5, current: 2 };
  reward: { points: 50, achievement: "thai_explorer" };
  endDate: "2024-12-01";
}
```

**Топы и рейтинги:**
- Лучшие критики недели
- Самые активные исследователи
- Восходящие звезды

**Система очков:**
- Отзыв: +10 points
- Фото: +5 points
- Добавление блюда: +15 points
- Достижение: +50-250 points

---

## 💡 Дополнительные идеи

### Voice AI (v3.0+)
```typescript
"Окей, FoodHub, что тут стоит попробовать?"
→ AI анализирует профиль + location + рейтинги
→ Голосовой ответ с рекомендациями
```

### Predictive Analytics
```typescript
// На основе похожих пользователей
"Пользователи с похожим вкусом также попробовали..."
```

### Heat Map
```typescript
// Карта лучших блюд по типу
"Лучшие бургеры города" → тепловая карта
```

---

## ✅ Готово к использованию

**Созданные файлы:**
- ✅ `src/types/profile.ts` - все типы
- ✅ `src/services/achievementService.ts` - достижения
- ✅ `src/services/recommendationService.ts` - AI рекомендации + сомелье
- ✅ `src/services/tasteProfileService.ts` - вкусовые профили
- ✅ `src/components/AchievementBadge.tsx` - UI достижений
- ✅ `src/components/PairingCard.tsx` - UI сомелье
- ✅ `src/screens/TasteProfileScreen.tsx` - настройка профиля

**Что добавить в следующей сборке:**
1. Обновить RootStackParamList
2. Добавить TasteProfileScreen в навигатор
3. Интегрировать PairingCard в DishDetailScreen
4. Добавить AchievementBadge в ProfileScreen
5. Добавить проверку достижений после действий

---

**Версия:** 2.1 (AI Features)  
**Статус:** 🎨 UI готов, требуется интеграция  
**Следующая версия:** 2.1.1 (полная интеграция)

