# Руководство по внесению вклада в FoodHub

Спасибо за интерес к улучшению FoodHub! 🎉

## Как внести вклад

### Сообщение об ошибках

Если вы нашли баг:

1. Проверьте, не был ли он уже сообщен в Issues
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Версия приложения и платформа

### Предложение новых функций

1. Откройте Issue с описанием функции
2. Объясните, почему это будет полезно
3. Приведите примеры использования

### Внесение изменений в код

1. **Fork репозитория**
2. **Создайте ветку для вашей функции**
   ```bash
   git checkout -b feature/amazing-feature
   ```

3. **Следуйте стандартам кода**
   - Используйте TypeScript
   - Следуйте существующему стилю кода
   - Добавляйте комментарии к сложной логике
   - Используйте осмысленные имена переменных

4. **Коммиты**
   ```bash
   git commit -m "feat: add amazing feature"
   ```
   
   Формат сообщений коммитов:
   - `feat:` - новая функция
   - `fix:` - исправление бага
   - `docs:` - изменения в документации
   - `style:` - форматирование, отступы
   - `refactor:` - рефакторинг кода
   - `test:` - добавление тестов
   - `chore:` - обновление зависимостей и т.д.

5. **Push в ваш Fork**
   ```bash
   git push origin feature/amazing-feature
   ```

6. **Создайте Pull Request**

## Стандарты кода

### TypeScript

- Всегда указывайте типы для функций
- Используйте интерфейсы для объектов
- Избегайте `any` где возможно

```typescript
// ✅ Хорошо
interface User {
  id: string;
  name: string;
}

const getUser = (id: string): Promise<User> => {
  // ...
}

// ❌ Плохо
const getUser = (id: any): any => {
  // ...
}
```

### Компоненты React

- Используйте функциональные компоненты
- Типизируйте props
- Разделяйте логику и представление

```typescript
// ✅ Хорошо
interface ButtonProps {
  title: string;
  onPress: () => void;
  disabled?: boolean;
}

const Button: React.FC<ButtonProps> = ({ title, onPress, disabled }) => {
  return (
    <TouchableOpacity onPress={onPress} disabled={disabled}>
      <Text>{title}</Text>
    </TouchableOpacity>
  );
};

// ❌ Плохо
const Button = (props: any) => {
  return <TouchableOpacity onPress={props.onPress}><Text>{props.title}</Text></TouchableOpacity>;
};
```

### Стили

- Используйте StyleSheet.create
- Группируйте связанные стили
- Используйте Theme константы

```typescript
// ✅ Хорошо
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Theme.colors.background,
    padding: Theme.spacing.md,
  },
  title: {
    fontSize: Theme.fontSize.xl,
    fontWeight: Theme.fontWeight.bold,
    color: Theme.colors.text,
  },
});

// ❌ Плохо
const styles = {
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF',
    padding: 16,
  },
};
```

### Именование файлов

- Компоненты: PascalCase (`Button.tsx`, `UserCard.tsx`)
- Утилиты: camelCase (`validation.ts`, `formatters.ts`)
- Константы: camelCase (`colors.ts`, `theme.ts`)

## Структура Pull Request

### Хороший PR содержит:

1. **Четкое описание изменений**
2. **Связь с Issue** (если применимо)
3. **Скриншоты** (для UI изменений)
4. **Тесты** (когда необходимо)
5. **Обновленную документацию**

### Шаблон описания PR:

```markdown
## Описание
Краткое описание того, что делает этот PR

## Связанные Issues
Closes #123

## Изменения
- Добавлена функция X
- Исправлен баг Y
- Обновлена документация Z

## Скриншоты (если применимо)
[Добавьте скриншоты здесь]

## Чеклист
- [ ] Код следует стандартам проекта
- [ ] Все тесты проходят
- [ ] Документация обновлена
- [ ] Нет конфликтов с main
```

## Процесс ревью

1. Maintainer проверит ваш PR
2. Могут быть запрошены изменения
3. После одобрения PR будет смержен

## Вопросы?

Если у вас есть вопросы, не стесняйтесь:
- Открыть Issue с вопросом
- Написать в Discussions

Спасибо за ваш вклад! 🙏

