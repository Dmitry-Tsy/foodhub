# 📱 Руководство по установке FoodHub

## 🎯 Выберите вашу версию

У вас есть **2 версии APK** на выбор:

---

## 📦 ВЕРСИЯ 1: Для Android Emulator (РЕКОМЕНДУЕТСЯ ДЛЯ ТЕСТИРОВАНИЯ)

### **Файл:** `FoodHub-EMULATOR.apk`

### **API URL:** `http://10.0.2.2:3000/api`
(10.0.2.2 - это специальный адрес, который эмулятор использует для доступа к localhost компьютера)

### **Преимущества:**
✅ **Работает сразу** - не нужна настройка сети  
✅ **Стабильно** - нет проблем с Wi-Fi  
✅ **Просто** - установил и работает  

### **Установка:**

```bash
# 1. Запустите бэкенд
cd backend
npm run dev

# 2. Запустите Android Emulator
# (через Android Studio или командой)

# 3. Установите APK
adb install FoodHub-EMULATOR.apk

# 4. Готово! Используйте приложение
```

### **Проверка:**
```bash
# В эмуляторе откройте браузер и перейдите:
http://10.0.2.2:3000/health

# Должно показать JSON
```

---

## 📦 ВЕРСИЯ 2: Для реального устройства

### **Файл:** `FoodHub-REAL-DEVICE.apk`

### **API URL:** `http://192.168.31.212:3000/api`

### **Требования:**
⚠️ **Устройство и компьютер должны быть в одной Wi-Fi сети!**

### **Установка:**

```bash
# 1. Запустите бэкенд
cd backend
npm run dev

# 2. ВАЖНО: Проверьте что устройство и компьютер в одной Wi-Fi сети

# 3. Проверьте IP компьютера
ifconfig | grep "inet " | grep -v 127.0.0.1
# Должно быть: 192.168.31.212

# 4. Проверьте доступность (на телефоне в браузере):
# Откройте: http://192.168.31.212:3000/health
# Если НЕ открывается - разные сети!

# 5. Установите APK
adb install FoodHub-REAL-DEVICE.apk

# 6. Готово!
```

---

## 🔍 Диагностика проблем

### **❌ Ошибка: "Не удается подключиться к серверу"**

#### **Для ЭМУЛЯТОРА:**

1. **Проверьте что бэкенд запущен:**
   ```bash
   curl http://localhost:3000/health
   ```
   
2. **Если не работает:**
   - Запустите: `cd backend && npm run dev`
   - Подождите пока появится "Server running on port 3000"

#### **Для РЕАЛЬНОГО УСТРОЙСТВА:**

1. **Откройте браузер на телефоне**
   ```
   http://192.168.31.212:3000/health
   ```

2. **Если открывается JSON:**
   ✅ Сеть работает!
   ➡️ Переустановите APK: `adb install -r FoodHub-REAL-DEVICE.apk`

3. **Если НЕ открывается:**
   ❌ Устройство и компьютер в разных сетях!
   
   **Решение:**
   - На Mac: Системные настройки → Wi-Fi → Посмотрите сеть
   - На телефоне: Settings → Wi-Fi → Посмотрите сеть
   - **Подключите к одной и той же сети!**

---

## 🧪 Встроенный тест connectivity

В приложении есть встроенный инструмент диагностики!

### **Как использовать:**

1. Откройте FoodHub
2. Перейдите на вкладку **Profile**
3. Нажмите **🔧 Тест подключения**
4. Нажмите **"Запустить тесты"**

### **Что проверяется:**

- ✅ Health check
- ✅ API search
- ✅ Регистрация

**Результаты:**
- 🟢 Зеленый блок = тест пройден
- 🔴 Красный блок = ошибка (с подробностями)

---

## 💡 Рекомендации

### **Для тестирования и разработки:**

➡️ **Используйте `FoodHub-EMULATOR.apk`**
- Не требует настройки сети
- Работает стабильно
- Проще отлаживать

### **Для демонстрации на реальном устройстве:**

➡️ **Используйте `FoodHub-REAL-DEVICE.apk`**
- Но убедитесь что Wi-Fi настроен правильно
- Проверьте через браузер сначала

---

## 🚀 Быстрый старт

### **Вариант A: Эмулятор (проще)**

```bash
# 1. Запустите бэкенд
cd backend && npm run dev

# 2. Запустите эмулятор и установите
adb install FoodHub-EMULATOR.apk

# 3. Готово! Регистрируйтесь и тестируйте
```

### **Вариант B: Реальное устройство**

```bash
# 1. Запустите бэкенд
cd backend && npm run dev

# 2. ПРОВЕРЬТЕ в браузере телефона:
# http://192.168.31.212:3000/health

# 3. Если JSON показывается - установите APK:
adb install FoodHub-REAL-DEVICE.apk

# 4. Если НЕ показывается - подключите к одной Wi-Fi сети!
```

---

## 🔧 Частые проблемы

### **1. "Network Error" в приложении**

**Причина:** Устройство не может подключиться к компьютеру

**Решение:**
- Проверьте Wi-Fi (должна быть одна сеть)
- Используйте эмулятор вместо реального устройства

### **2. IP адрес изменился**

**Причина:** Роутер дал другой IP компьютеру

**Как узнать:**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**Если изменился:**
- Используйте эмулятор (не зависит от IP)
- Или пересоберите APK с новым IP

### **3. Брандмауэр блокирует**

**Симптомы:**
- Localhost работает
- Но IP не работает (даже в браузере)

**Решение:**
```bash
# Временно отключите брандмауэр для теста
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off

# После теста включите обратно
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate on
```

---

## 📊 Сравнение версий

| Параметр | Эмулятор | Реальное устройство |
|----------|----------|---------------------|
| **API URL** | 10.0.2.2:3000 | 192.168.31.212:3000 |
| **Настройка сети** | ❌ Не нужна | ✅ Нужна одна Wi-Fi |
| **Стабильность** | ✅ Высокая | ⚠️ Зависит от сети |
| **Сложность** | 😊 Легко | 😐 Средне |
| **Рекомендуется для** | Разработка | Демо/Продакшен |

---

## 🎯 Итог

### **МОЯ РЕКОМЕНДАЦИЯ:**

🎯 **Используйте эмулятор** для тестирования:

```bash
cd backend && npm run dev
adb install FoodHub-EMULATOR.apk
```

**Это:**
- ✅ Проще
- ✅ Быстрее
- ✅ Надежнее
- ✅ Не требует настройки Wi-Fi

Когда всё будет работать на эмуляторе, можно перейти к реальному устройству.

---

## 📞 Поддержка

**Если проблемы остались:**

1. Запустите встроенный тест в приложении
2. Сделайте скриншот результатов
3. Проверьте логи: `adb logcat | grep "📡\|❌"`

---

**Файлы:**
- `FoodHub-EMULATOR.apk` - для эмулятора ✅ РЕКОМЕНДУЕТСЯ
- `FoodHub-REAL-DEVICE.apk` - для реального устройства
- `CONNECTIVITY_DEBUG.md` - детальная диагностика

