# 🐛 Просмотр логов FoodHub

## 📱 Как увидеть что происходит в приложении

### Способ 1: Android Studio Logcat (рекомендуется)

1. **Подключите устройство** через USB
2. **Откройте Android Studio**
3. **View → Tool Windows → Logcat**
4. **Установите фильтр:** `package:com.foodhub.app`
5. **Запустите приложение** на устройстве
6. **Смотрите логи** в реальном времени!

### Способ 2: ADB logcat (через терминал)

```bash
# Подключите устройство через USB
# Включите USB отладку на устройстве

# Просмотр всех логов FoodHub
adb logcat | grep "FoodHub\|ReactNativeJS"

# Или с цветным выводом
adb logcat -v color | grep "ReactNativeJS"

# Сохранить логи в файл
adb logcat > foodhub-logs.txt
```

---

## 📊 Что искать в логах

### Загрузка ресторанов:

```
SearchScreen mounted, loading location...
🔍 Starting loadLocation...
📍 Requesting current location...
✅ Location received: {latitude: ..., longitude: ...}
🔄 Fetching nearby restaurants...
🏪 getNearbyRestaurants called with: ...
🗺️ Загрузка реальных ресторанов из Google Places API...
✅ Google Places вернул: X ресторанов
✅ Restaurants loaded: X
```

### Fallback на mock данные:

```
⚠️ Геолокация недоступна, используем mock координаты
🔄 Fetching restaurants with mock location...
📋 Google Places недоступен, используем mock данные
📋 Используем mock рестораны, всего: 3
✅ Mock restaurants loaded: 3
```

### Ошибки:

```
❌ Failed to load mock restaurants: ...
⚠️ Google Places API недоступен, используем mock данные: ...
```

---

## 🔍 Debug информация на экране

В **debug APK** на экране поиска видна черная полоска сверху с информацией:

```
Restaurants: 3 | Loading: NO | Location: YES
```

Это показывает:
- Сколько ресторанов загружено
- Идет ли загрузка
- Есть ли геолокация

---

## 📦 Доступные APK для дебага

### FoodHub-v1.2.1-Debug.apk (Release с логами)
- Размер: 63 MB
- console.log() работает
- Можно смотреть в logcat
- Подписан debug keystore

### FoodHub-v1.2.1-Debug-Verbose.apk (Debug build)
- Размер: 156 MB (не оптимизирован)
- Все логи включены
- Показывает debug полоску на экране
- Лучше для глубокого дебага

---

## 🎯 Проверка работы поиска ресторанов

### Шаг 1: Установите APK

```bash
adb install FoodHub-v1.2.1-Debug.apk
```

### Шаг 2: Запустите logcat

```bash
adb logcat | grep "ReactNativeJS"
```

### Шаг 3: Откройте приложение

1. Продолжите как гость или войдите
2. Перейдите на вкладку "Поиск"

### Шаг 4: Смотрите логи

Должны появиться логи:
```
SearchScreen mounted, loading location...
🔍 Starting loadLocation...
```

### Шаг 5: Анализ

**Если видите:**
- `✅ Restaurants loaded: 3` → Все работает!
- `❌ Failed to load` → Есть ошибка, смотрите сообщение
- Ничего не видите → Проверьте что logcat подключен

---

## 🐛 Возможные проблемы и решения

### Проблема: "Рестораны не загружаются"

**Проверьте логи на:**
```
🏪 getNearbyRestaurants called with: ...
```

Если этого нет → проблема в dispatch

**Решение:**
- Проверьте что Redux store правильно подключен
- Проверьте App.tsx - есть Provider?

### Проблема: "Google Places API error"

**Логи покажут:**
```
⚠️ Google Places API недоступен: Request failed with status 403
```

**Причины:**
- API ключ неверный
- Places API не включен в Google Cloud
- Превышен лимит запросов

**Решение:**
- Проверьте API ключ
- Включите Places API в console.cloud.google.com
- Приложение автоматически переключится на mock данные

### Проблема: "Геолокация не работает"

**Логи:**
```
⚠️ Геолокация недоступна, используем mock координаты
```

**Решение:**
- Разрешите геолокацию в настройках Android
- Приложение покажет рестораны в Москве (fallback)

---

## 💡 Быстрый тест

Самый простой способ проверить:

```bash
# Терминал 1: Логи
adb logcat | grep "ReactNativeJS"

# Терминал 2: Установка и запуск
adb install FoodHub-v1.2.1-Debug.apk
adb shell am start -n com.foodhub.app/.MainActivity
```

Затем смотрите логи в Терминале 1!

---

## 📊 Что показывают логи

Полный flow загрузки ресторанов выглядит так:

```
1. SearchScreen mounted, loading location...
2. 🔍 Starting loadLocation...
3. 📍 Requesting current location...
4. ✅ Location received: {lat, lng}
5. 🔄 Fetching nearby restaurants...
6. 🏪 getNearbyRestaurants called with: ...
7. 🗺️ Загрузка реальных ресторанов из Google Places API...
8. ✅ Google Places вернул: N ресторанов
9. ✅ Restaurants loaded: N
10. 📊 SearchScreen state: {restaurantsCount: N, ...}
```

Если прерывается на каком-то шаге → там проблема!

---

**Установите debug APK и проверьте логи!** 🔍

