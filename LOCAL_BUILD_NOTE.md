# 📝 Примечание о локальной сборке

## Проблема с expo-linear-gradient

При локальной сборке через Gradle возникает конфликт с `expo-module-gradle-plugin`, который требуется для `expo-linear-gradient`.

### Решение:

**Для локальной сборки:** Градиенты временно отключены (используются solid цвета)  
**Для production:** Используйте EAS Build в облаке - там все градиенты работают

### Команда для cloud build:

```bash
eas build --platform android --profile preview
```

### Альтернатива:

В коде используйте fallback на solid цвета:

```typescript
// Вместо LinearGradient:
<View style={{ backgroundColor: Colors.primary }}>
  {/* content */}
</View>
```

Все градиенты уже реализованы в коде, просто для локальной разработки/тестирования используются solid цвета.

