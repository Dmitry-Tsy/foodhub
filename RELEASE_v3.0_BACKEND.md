# 🚀 FoodHub v3.0 - Backend Integration

## 📦 Release Date: November 11, 2025

---

## 🎯 Основные изменения

### ✅ **Полная интеграция с бэкендом**

#### **Фронтенд обновления:**
- ✅ `authService.ts` - подключен к REST API
- ✅ `dishService.ts` - подключен к REST API  
- ✅ `reviewService.ts` - подключен к REST API
- ✅ `tasteProfileService.ts` - подключен к REST API
- ✅ Удален весь mock код и тестовые данные
- ✅ API клиент с JWT авторизацией

#### **Бэкенд реализация:**
- ✅ Node.js + Express + TypeScript
- ✅ PostgreSQL база данных
- ✅ JWT авторизация с защищенными маршрутами
- ✅ 8 моделей данных (Users, Dishes, Reviews, Restaurants, etc.)
- ✅ 4 группы API эндпоинтов

---

## 📡 API Endpoints

### **1. Authentication (`/api/auth`)**
```
POST   /api/auth/register     - Регистрация нового пользователя
POST   /api/auth/login        - Вход в систему
GET    /api/auth/profile      - Получить профиль текущего пользователя
PUT    /api/auth/profile      - Обновить профиль
```

### **2. Dishes (`/api/dishes`)**
```
GET    /api/dishes/restaurant/:id  - Получить меню ресторана
GET    /api/dishes/:id             - Получить блюдо по ID
POST   /api/dishes                 - Добавить новое блюдо (требует auth)
PUT    /api/dishes/:id             - Обновить блюдо (требует auth)
DELETE /api/dishes/:id             - Удалить блюдо (требует auth)
```

### **3. Reviews (`/api/dishes/:dishId/reviews`)**
```
GET    /api/dishes/:dishId/reviews  - Получить отзывы на блюдо
POST   /api/dishes/:dishId/reviews  - Добавить отзыв (требует auth)
PUT    /api/reviews/:id             - Обновить отзыв (требует auth)
DELETE /api/reviews/:id             - Удалить отзыв (требует auth)
```

### **4. Restaurants (`/api/restaurants`)**
```
GET    /api/restaurants/:id         - Получить ресторан по ID
GET    /api/restaurants/search      - Поиск ресторанов
POST   /api/restaurants             - Создать/получить ресторан (требует auth)
```

### **5. Taste Profiles (`/api/taste-profile`)**
```
GET    /api/taste-profile           - Получить вкусовой профиль (требует auth)
POST   /api/taste-profile           - Создать/обновить профиль (требует auth)
DELETE /api/taste-profile           - Удалить профиль (требует auth)
```

---

## 🗄️ База данных

### **Таблицы PostgreSQL:**
1. **users** - Пользователи приложения
2. **restaurants** - Рестораны (из Google Places + локальные)
3. **dishes** - Блюда в меню
4. **dish_reviews** - Отзывы на блюда
5. **taste_profiles** - Вкусовые профили пользователей
6. **favorites** - Избранные рестораны/блюда
7. **follows** - Подписки между пользователями
8. **user_achievements** - Достижения пользователей

---

## ⚙️ Конфигурация

### **API URL (Frontend)**
Файл: `src/services/api.ts`
```typescript
const API_BASE_URL = 'http://10.0.2.2:3000/api'; // Для эмулятора
// или
const API_BASE_URL = 'http://192.168.x.x:3000/api'; // Для реального устройства
```

### **Переменные окружения (Backend)**
Файл: `backend/.env`
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=foodhub
DB_USER=dmitrytsymlyakov
DB_PASSWORD=foodhub2025

JWT_SECRET=foodhub_super_secret_key_2025_very_long_and_secure
JWT_EXPIRES_IN=7d

PORT=3000
NODE_ENV=development
```

---

## 🚀 Запуск

### **1. Запустить бэкенд:**
```bash
cd backend
npm install
npm run dev
```

Бэкенд будет доступен на `http://localhost:3000`

### **2. Установить APK:**
```bash
adb install FoodHub-v3.0-BACKEND.apk
```

### **3. Проверка health check:**
```bash
curl http://localhost:3000/health
```

---

## 📊 Статистика проекта

- **Frontend:** 61 TypeScript файл
- **Backend:** 17 TypeScript файл
- **APK размер:** ~64 MB
- **Эндпоинты:** 20+ REST API endpoints
- **Модели БД:** 8 таблиц

---

## 🔧 Технологии

### **Frontend:**
- React Native
- Redux Toolkit
- TypeScript
- Axios (HTTP клиент)
- AsyncStorage (локальное хранилище)
- React Native Keychain (безопасное хранение токенов)

### **Backend:**
- Node.js + Express
- TypeScript
- PostgreSQL + Sequelize ORM
- JWT (jsonwebtoken)
- bcryptjs (хеширование паролей)
- Helmet + CORS (безопасность)

---

## 🐛 Известные проблемы

- ⚠️ Для работы требуется запущенный бэкенд сервер
- ⚠️ На реальном устройстве нужно изменить API_BASE_URL на IP компьютера
- ⚠️ Отзывы и блюда теперь хранятся только в БД (не в локальном кеше)

---

## 📝 Миграция с v2.2

1. **Удалите старую версию** приложения (чтобы очистить локальный кеш)
2. **Запустите PostgreSQL** и создайте БД `foodhub`
3. **Запустите бэкенд** сервер
4. **Установите новый APK**
5. **Зарегистрируйтесь заново** (старые mock пользователи не работают)

---

## 🎉 Что дальше?

### Возможные улучшения:
- [ ] Загрузка изображений на Cloudinary
- [ ] Push-уведомления
- [ ] Реальный AI-сомелье (интеграция с OpenAI)
- [ ] Геолокационный поиск ресторанов
- [ ] Социальные функции (лента, комментарии)
- [ ] Версионирование API
- [ ] Docker-контейнеры для бэкенда
- [ ] CI/CD пайплайн

---

**Разработано с ❤️ для FoodHub**

