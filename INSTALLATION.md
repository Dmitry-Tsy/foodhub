# Инструкция по установке FoodHub

## Шаг 1: Установка зависимостей

В директории проекта выполните:

```bash
npm install
```

или

```bash
yarn install
```

## Шаг 2: Установка Expo CLI (если еще не установлен)

```bash
npm install -g expo-cli
```

## Шаг 3: Создание asset файлов

Создайте следующие файлы в директории `assets/`:
- `icon.png` (1024x1024) - иконка приложения
- `splash.png` (1284x2778) - экран загрузки
- `adaptive-icon.png` (1024x1024) - адаптивная иконка для Android
- `favicon.png` (48x48) - favicon для web версии

Вы можете использовать любые изображения или создать простые placeholder'ы.

## Шаг 4: Запуск приложения

### Режим разработки (Development)

```bash
npm start
```

Это запустит Expo Dev Server. После этого вы можете:
- Нажать `i` для запуска iOS симулятора
- Нажать `a` для запуска Android эмулятора
- Отсканировать QR код в приложении Expo Go на реальном устройстве

### Запуск на iOS

```bash
npm run ios
```

Требования:
- macOS
- Xcode
- iOS Simulator

### Запуск на Android

```bash
npm run android
```

Требования:
- Android Studio
- Android SDK
- Настроенный эмулятор или подключенное устройство

### Запуск Web версии

```bash
npm run web
```

## Шаг 5: Настройка переменных окружения (опционально)

Создайте файл `.env` в корне проекта:

```env
API_BASE_URL=http://localhost:3000/api
GOOGLE_PLACES_API_KEY=your_api_key_here
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
```

## Тестовые данные

Приложение использует mock данные для разработки. Для входа используйте:

**Email:** foodlover@example.com  
**Пароль:** любой пароль (минимум 6 символов)

или

**Email:** chef@example.com  
**Пароль:** любой пароль (минимум 6 символов)

## Решение проблем

### Проблема: Metro bundler не запускается

```bash
npm start -- --reset-cache
```

### Проблема: Ошибка с зависимостями

```bash
rm -rf node_modules
npm install
```

### Проблема: iOS симулятор не запускается

Убедитесь, что Xcode установлен и Command Line Tools настроены:

```bash
xcode-select --install
```

### Проблема: Android эмулятор не запускается

1. Откройте Android Studio
2. Tools -> AVD Manager
3. Создайте новое виртуальное устройство
4. Запустите эмулятор

### Проблема: Expo не может найти устройство

Убедитесь, что ваше устройство и компьютер находятся в одной Wi-Fi сети.

## Структура проекта после установки

```
FoodHub/
├── node_modules/         # Установленные пакеты
├── assets/              # Изображения и ресурсы
├── src/                 # Исходный код
├── App.tsx             # Точка входа
├── package.json        # Зависимости
├── tsconfig.json       # TypeScript конфиг
├── app.json           # Expo конфиг
└── babel.config.js    # Babel конфиг
```

## Полезные команды

```bash
# Проверка TypeScript
npm run type-check

# Линтинг
npm run lint

# Очистка кеша
expo start -c

# Билд для продакшн
expo build:ios
expo build:android
```

## Следующие шаги

1. Изучите структуру проекта в `/src`
2. Ознакомьтесь с mock данными в `/src/services/mockData.ts`
3. Попробуйте различные функции приложения
4. Настройте реальный backend (см. README.md)

Удачи в разработке! 🚀

