# 🔧 Диагностика проблем connectivity

## ❌ Ошибка: "Не удается подключиться к серверу"

---

## ✅ Что уже проверено

**Бэкенд работает правильно:**
- ✅ Health check: http://localhost:3000/health - OK
- ✅ Health check через IP: http://192.168.31.212:3000/health - OK
- ✅ Порт 3000 слушается
- ✅ Процесс npm run dev запущен
- ✅ API эндпоинты работают

**Проблема НЕ в бэкенде!** Проблема в connectivity между устройством и компьютером.

---

## 🔍 Пошаговая диагностика

### **Шаг 1: Проверьте Wi-Fi сеть**

**На компьютере (Mac):**
```bash
# Узнать к какой сети подключен
networksetup -getairportnetwork en0

# Узнать IP адрес
ifconfig | grep "inet " | grep -v 127.0.0.1
# Должно быть: 192.168.31.212
```

**На Android устройстве:**
1. Настройки → Wi-Fi
2. Посмотрите к какой сети подключены
3. **ВАЖНО:** Должна быть та же сеть что и на компьютере!

---

### **Шаг 2: Проверьте доступность с устройства**

**Откройте браузер на телефоне (Chrome/Firefox):**

```
http://192.168.31.212:3000/health
```

**Что должно произойти:**

✅ **Если работает:**
```json
{
  "status": "ok",
  "timestamp": "2025-11-11T...",
  "uptime": 123.45
}
```
➡️ **Отлично! Connectivity работает. Проблема в приложении.**

❌ **Если не работает:**
```
This site can't be reached
192.168.31.212 refused to connect
```
➡️ **Проблема в сети! См. решения ниже.**

---

### **Шаг 3: Используйте встроенный тест**

**В приложении FoodHub:**

1. Установите `FoodHub-v3.1-CONNECTIVITY.apk`
2. Откройте приложение
3. Зайдите в раздел **Profile**
4. Нажмите **🔧 Тест подключения**
5. Нажмите **"Запустить тесты"**

**Результаты покажут:**
- ✅ Зеленые блоки - тест пройден
- ❌ Красные блоки - проблема

---

## 🔧 Решения проблем

### **Проблема 1: Разные Wi-Fi сети**

**Симптомы:**
- Браузер на телефоне не открывает `http://192.168.31.212:3000/health`
- Ошибка: "This site can't be reached"

**Решение:**
1. Подключите устройство и компьютер к **одной** Wi-Fi сети
2. Перезапустите приложение
3. Попробуйте снова

---

### **Проблема 2: Брандмауэр (Firewall) блокирует**

**Симптомы:**
- Localhost работает (http://localhost:3000/health)
- Но IP не работает (http://192.168.31.212:3000/health)

**Решение для macOS:**

```bash
# Проверить состояние брандмауэра
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate

# Разрешить Node.js
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/local/bin/node

# Или временно отключить брандмауэр (не рекомендуется)
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off
```

**Альтернатива - через System Settings:**
1. System Settings → Network → Firewall
2. Firewall Options
3. Добавьте Node.js в разрешенные приложения

---

### **Проблема 3: IP адрес изменился**

**Симптомы:**
- Раньше работало, теперь не работает
- Другая Wi-Fi сеть или перезагрузка роутера

**Решение:**

1. **Узнайте новый IP:**
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```

2. **Обновите в коде:**
   ```typescript
   // src/services/api.ts
   const API_BASE_URL = 'http://НОВЫЙ_IP:3000/api';
   ```

3. **Пересоберите APK:**
   ```bash
   cd android
   ./gradlew assembleRelease
   adb install app/build/outputs/apk/release/app-release.apk
   ```

---

### **Проблема 4: Порт 3000 занят**

**Проверка:**
```bash
lsof -i :3000
```

**Если порт занят другим процессом:**
```bash
# Убить процесс
kill -9 PID_ПРОЦЕССА

# Или использовать другой порт
# в backend/.env:
PORT=3001

# И в api.ts:
const API_BASE_URL = 'http://192.168.31.212:3001/api';
```

---

### **Проблема 5: Таймаут слишком короткий**

**Симптомы:**
- Ошибка: "ECONNABORTED" или "timeout of 10000ms exceeded"
- Медленная сеть

**Решение:**

Увеличьте timeout в `src/services/api.ts`:
```typescript
this.api = axios.create({
  baseURL: API_BASE_URL,
  timeout: 30000,  // 30 секунд вместо 10
  headers: {
    'Content-Type': 'application/json',
  },
});
```

---

## 📱 Встроенный тест в приложении

### **Как использовать:**

1. Откройте FoodHub
2. Войдите или продолжите как гость
3. Перейдите на вкладку **Profile**
4. Нажмите **🔧 Тест подключения**
5. Нажмите **"Запустить тесты"**

### **Что тестируется:**

**Test 1: Health Check**
```
GET http://192.168.31.212:3000/health
Timeout: 5 секунд
```

✅ Успех: Сервер доступен  
❌ Ошибка: Проверьте сеть

**Test 2: API Search**
```
GET http://192.168.31.212:3000/api/search?query=test
Timeout: 5 секунд
```

✅ Успех: API работает  
❌ Ошибка: Проблема с API

**Test 3: Registration**
```
POST http://192.168.31.212:3000/api/auth/register
Timeout: 10 секунд
```

✅ Успех: Регистрация работает  
❌ Ошибка: Проблема с БД или API

---

## 🧪 Ручное тестирование

### **Через браузер на телефоне:**

1. Откройте Chrome/Firefox
2. Перейдите: `http://192.168.31.212:3000/health`
3. Должно показать JSON

### **Через curl (на компьютере):**

```bash
# Проверка через IP (как будто с устройства)
curl http://192.168.31.212:3000/health

# Должно вернуть:
{
  "status": "ok",
  ...
}
```

### **Через adb logcat:**

```bash
# Запустите приложение и смотрите логи
adb logcat | grep "📡\|❌\|Network\|connectivity"
```

Ищите строки типа:
```
📡 API URL: http://192.168.31.212:3000/api/auth/login
❌ Ошибка авторизации: Network Error
```

---

## 💡 Чеклист

Пройдите по этому чеклисту:

- [ ] **Бэкенд запущен** - `cd backend && npm run dev`
- [ ] **Health check работает** - `curl http://localhost:3000/health`
- [ ] **IP правильный** - `192.168.31.212`
- [ ] **Устройство и компьютер в одной Wi-Fi сети**
- [ ] **Браузер на телефоне открывает** - `http://192.168.31.212:3000/health`
- [ ] **Брандмауэр разрешает** - порт 3000 открыт
- [ ] **APK актуальный** - `FoodHub-v3.1-CONNECTIVITY.apk`

---

## 🎯 Частые причины

| Проблема | Вероятность | Решение |
|----------|-------------|---------|
| Разные Wi-Fi сети | 60% | Подключите к одной сети |
| Брандмауэр блокирует | 20% | Разрешите Node.js |
| IP адрес изменился | 10% | Обновите в api.ts |
| Бэкенд не запущен | 5% | npm run dev |
| Неправильный порт | 3% | Проверьте 3000 |
| Другое | 2% | См. логи |

---

## 📞 Быстрая помощь

**1. Проверка с компьютера:**
```bash
curl http://192.168.31.212:3000/health
```

**2. Проверка с телефона:**
```
Откройте браузер: http://192.168.31.212:3000/health
```

**3. Если работает в браузере, но не в приложении:**
- Переустановите APK
- Очистите кеш приложения
- Проверьте логи: `adb logcat`

**4. Если не работает нигде:**
- Проверьте Wi-Fi сети
- Отключите брандмауэр временно
- Попробуйте другой IP адрес

---

**APK с тестом: FoodHub-v3.1-CONNECTIVITY.apk**

